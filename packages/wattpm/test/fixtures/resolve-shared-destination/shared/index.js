// The destination already holds one of the two clones, which is the case the refusal is for: a run
// that filtered by existence first would drop the evidence and resolve nothing.
export default function () {}
