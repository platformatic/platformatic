{
  "name": "app-with-config",
  "scripts": {
    "clean": "rm -fr ./dist",
    "build": "tsc -p ."
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@platformatic/globals": ">=3.0.0",
    "@platformatic/service": ">=3.0.0"
  }
}
