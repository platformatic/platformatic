// Converted from v3 JSON by scripts/convert-fixtures.mjs
export default {
  // v3 declared services: [] -- an explicit empty list, which is allowed, where an absent one is a
  // refusal.
  applications: [],
  gracefulShutdown: {
    runtime: 1000,
    application: 1000
  }
}
