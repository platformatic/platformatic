import Issues from '../../getting-started/issues.md';

# Configuration

Platformatic TanStack is configured with a configuration file. The file is a module that exports its configuration, so it reads
[environment variables](../service/configuration.md#environment-variables) directly.

It supports all the [settings supported by Platformatic Vite](../vite/configuration.md).

<Issues />
