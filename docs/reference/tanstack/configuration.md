import Issues from '../../getting-started/issues.md';

# Configuration

Platformatic TanStack is configured with a configuration file. It supports the use
of environment variables as setting values with [configuration placeholders](#configuration-placeholders).

It supports all the [settings supported by Platformatic Vite](../vite/configuration.md).

<Issues />
